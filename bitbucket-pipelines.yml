image: arminvieweg/php-code-checks:latest

pipelines:
  default:
    - step:
        script:
          - phpcs --extensions=php --standard=PSR2 ./Classes ./*.php
          - phpmd ./Classes text cleancode,design,codesize,naming,unusedcode --ignore-violations-on-exit
          - phploc ./Classes